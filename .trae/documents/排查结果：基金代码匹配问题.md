## 修改内容

### 1. 修改API类型定义 (`app/src/services/api.ts`)
将 `getDetail` 的返回类型从 `{ basic_info: FundInfo; latest_nav: FundNav; metrics: any }` 改为 `FundInfo`，与后端实际返回的数据结构一致。

### 2. 修改 `index.tsx` 中的 `matchFunds` 函数
直接使用 `response.data` 而不是 `response.data.basic_info`。

### 3. 修改 `ImageImport.tsx` 中的 `matchFunds` 函数（如果存在）
同样修复API调用和数据访问方式。

这样修改后，前端就能正确匹配所有基金代码了。